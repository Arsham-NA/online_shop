
const Logo = ({className}) => {
	return (
		<div className={`text-xl font-semibold text-primary text-nowrap flex justify-center items-center gap-x-2 cursor-default ${className}`}><span className="text-white bg-primary px-2 rounded">O</span> Shop</div>
	);
};

export default Logo